{
  "name": "skanix",
  "version": "1.0.0",
  "description": "Skanix — escáner de documentos offline-first.",
  "private": true,
  "scripts": {
    "start": "npx serve docs -l 8080",
    "cap:sync": "npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/status-bar": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
